{
  "name": "protos",
  "private": true,
  "publishConfig": {
    "directory": "dist",
    "linkDirectory": false
  },
  "scripts": {
    "build": "ts-pkg-build build",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@protobuf-es/cli": "^0.0.22",
    "@cexoso/ts-pkg-build": "^0.0.15"
  }
}
